{
  "name": "@toolbox/cron-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo 'cron-worker — lint via root'",
    "typecheck": "tsc --noEmit",
    "build": "echo 'cron-worker — Vercel Cron deploys via apps/web/api/cron'",
    "dev": "echo 'use Vercel CLI for local cron'"
  },
  "dependencies": {
    "@toolbox/db": "workspace:*",
    "@toolbox/domain": "workspace:*",
    "@toolbox/notify": "workspace:*",
    "@toolbox/queue": "workspace:*"
  },
  "devDependencies": {
    "@toolbox/config": "workspace:*",
    "@types/node": "^20.16.0",
    "typescript": "^5.6.2"
  }
}
